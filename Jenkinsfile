pipeline {
    agent any
    stages {
        stage ('git repo'){
            steps {
                bat "rmdir /s /q asp-net-getting-started"
                bat "git clone https://"
            }
        }

    }
}